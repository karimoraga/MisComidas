import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ComidasprohibidasPage } from './comidasprohibidas.page';

describe('ComidasprohibidasPage', () => {
  let component: ComidasprohibidasPage;
  let fixture: ComponentFixture<ComidasprohibidasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ComidasprohibidasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
