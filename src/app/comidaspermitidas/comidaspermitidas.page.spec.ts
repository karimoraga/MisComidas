import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ComidaspermitidasPage } from './comidaspermitidas.page';

describe('ComidaspermitidasPage', () => {
  let component: ComidaspermitidasPage;
  let fixture: ComponentFixture<ComidaspermitidasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ComidaspermitidasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
